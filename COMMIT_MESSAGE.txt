feat: complete legacy API removal and documentation updates

Major Changes:
1. Remove frontend build artifacts from git tracking
   - Add feishu_bot/web_admin/static/ to .gitignore
   - Follow open source best practices
   - Reduce repository size

2. Remove obsolete configuration fields
   - Remove DEFAULT_PROVIDER (replaced by unified API)
   - Remove DEFAULT_LAYER (replaced by smart routing)
   - Clean up all references in backend, frontend, tests, and docs

3. Add Qwen CLI provider support
   - Add "qwen" to VALID_CLI_PROVIDERS list
   - Update frontend dropdown to include Qwen option
   - Update validation rules

4. Implement dynamic configuration reload
   - Add reload_if_changed() to ProviderConfigManager
   - Configuration changes now take effect without restart
   - Track file modification time for auto-reload

5. Update Docker deployment configuration
   - Change service name from feishu-bot-web-admin to feishu-bot
   - Update port from 5000 to 8080
   - Fix Dockerfile CMD to run full bot service
   - Update docker-compose.yml with correct environment variables

6. Make help messages configurable
   - Create feishu_bot/help_config/ directory
   - Add help_messages.json for structured help content
   - Add help_loader.py with fallback mechanism
   - Remove obsolete /layer command from help

7. Display specific model name in responses
   - Add get_current_provider_config() to UnifiedAPIInterface
   - Show provider and model name (e.g., "ModelScope (GLM-5)")
   - Replace generic "Unified API (@gpt)" with specific info

8. Fix Mermaid diagram rendering issues
   - Remove @ symbols from node labels
   - Simplify edge labels for better compatibility

9. Comprehensive documentation updates
   - Rewrite README.md with current architecture
   - Rewrite docs/INTRODUCTION.md with Mermaid diagrams
   - Rewrite docs/guides/USER_GUIDE.md with current commands
   - Update all deployment documentation
   - Remove references to obsolete features

10. Clean up obsolete files
    - Remove scripts/start_web_admin.sh
    - Remove scripts/start_all.sh and start_all.bat
    - Remove test_api.py
    - Remove obsolete executor files
    - Rename systemd service file

Breaking Changes:
- Users running from source must build frontend first:
  cd frontend && npm install && npm run build
- DEFAULT_PROVIDER and DEFAULT_LAYER environment variables removed
- /layer command removed (use Web admin to configure providers)
- Service name changed to feishu-bot (update systemd/docker configs)
- Default port changed to 8080 (update nginx/firewall configs)

Migration Guide:
1. Remove DEFAULT_PROVIDER and DEFAULT_LAYER from .env
2. Configure AI providers via Web admin interface (http://localhost:8080)
3. Update systemd service name if using systemd deployment
4. Update port mappings if using custom deployment
5. Build frontend if running from source

Files Changed:
- Backend: 20+ files (config, executors, web_admin, core modules)
- Frontend: 10+ files (views, components, tests)
- Documentation: 15+ files (guides, deployment, examples)
- Configuration: .env.example, .gitignore, docker configs
- Tests: Multiple test files updated
